# Roadmap

## Now

(empty)

## Next

- Evaluate abstracting project workflow files (ROADMAP.md, BACKLOG.md, DONE.md) into a dedicated CLI tool
- Add `--format json` flag to `tail` and `search` for machine-readable output

## Later

- Config file support (`~/.config/session-logger/config`) as documented in spec but not yet implemented
- `session_logger.py stats` command — entry counts, session durations, activity heatmap
