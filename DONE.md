# Done

## 2026-04-11 — Initial build (PR #1)

- `session_logger.py` CLI with `write`, `tail`, `ls`, `search` commands
- JSONL storage format with markdown display formatting
- Git auto-commit on write, auto-push on finish
- Reference skill templates for `start-session`, `save-session`, `finish-session`, `break-session`, `recover-session`
- Migration script for existing SESSION.md files (`scripts/migrate_session_md.py`)
- Test suite
- Spec and README
